# Generic x86-64 GCC Build

# Arch Defines
arch = "x86-64"
arch_bit = 2
arch_flags = ["-m64", "-mcmodel=kernel"]
arch_ldf = ["-melf_x86_64", "-z", "max-page-size=0x1000"]

# Build Defines
is_debug = false
use_clang = false